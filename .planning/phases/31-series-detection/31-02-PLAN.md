---
phase: 31-series-detection
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/app/api/cron/detect-series/route.ts
  - src/lib/scraper/extractor.ts
  - src/app/api/admin/trigger/[job]/route.ts
  - src/app/admin/_components/TriggerActions.tsx
  - src/lib/db/backfill-series.ts
  - vercel.json
autonomous: true
requirements:
  - SER-05
  - SER-06

must_haves:
  truths:
    - "A dedicated cron endpoint at /api/cron/detect-series runs series detection on schedule (10 min after scrape)"
    - "Gemini extraction prompt asks for recurrence_pattern hint from page content"
    - "Admin can trigger detect-series from the dashboard"
    - "A backfill script exists to seed series detection for all existing events"
  artifacts:
    - path: "src/app/api/cron/detect-series/route.ts"
      provides: "CRON_SECRET-protected GET endpoint calling detectAndTagSeries"
      exports: ["GET"]
    - path: "src/lib/db/backfill-series.ts"
      provides: "One-shot backfill script for existing events"
      min_lines: 10
    - path: "vercel.json"
      provides: "detect-series cron entry at 10 6 * * *"
      contains: "detect-series"
  key_links:
    - from: "src/app/api/cron/detect-series/route.ts"
      to: "src/lib/series-detector.ts"
      via: "imports detectAndTagSeries"
      pattern: "import.*detectAndTagSeries.*series-detector"
    - from: "src/app/api/admin/trigger/[job]/route.ts"
      to: "src/lib/series-detector.ts"
      via: "detect-series case imports detectAndTagSeries"
      pattern: "detect-series.*detectAndTagSeries"
    - from: "src/lib/db/backfill-series.ts"
      to: "src/lib/series-detector.ts"
      via: "calls detectAndTagSeries directly"
      pattern: "import.*detectAndTagSeries"
    - from: "src/lib/scraper/extractor.ts"
      to: "Gemini prompt"
      via: "recurrence_pattern instruction in extraction prompt"
      pattern: "recurrence_pattern"
---

<objective>
Wire the series detector into the system: dedicated cron endpoint, Gemini prompt update, admin trigger, and backfill script.

Purpose: The detection algorithm from Plan 01 needs integration points — a scheduled cron (separate from scrape to avoid timeout), the Gemini extraction prompt telling the AI to populate recurrence_pattern, an admin button for on-demand runs, and a backfill script for existing events.

Output: Complete series detection pipeline wired into the application.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-series-detection/31-RESEARCH.md
@.planning/phases/31-series-detection/31-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (series-detector.ts) -->
```typescript
export async function detectAndTagSeries(): Promise<{ seriesUpserted: number; eventsTagged: number }>;
```

<!-- Existing cron pattern (from /api/cron/archive/route.ts) -->
```typescript
export const maxDuration = 60;
export async function GET(request: Request): Promise<Response> {
  const authHeader = request.headers.get('authorization');
  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return Response.json({ success: false, error: 'Unauthorized' }, { status: 401 });
  }
  // ... call library function, return JSON
}
```

<!-- Admin trigger switch (from /api/admin/trigger/[job]/route.ts) -->
<!-- Add 'detect-series' case matching the pattern of existing cases -->

<!-- TriggerActions.tsx — add "Detect Series" button alongside existing buttons -->
<!-- formatSuccessMessage needs a detect-series case -->

<!-- Backfill pattern (from backfill-categories.ts) -->
```typescript
import 'dotenv/config';
// call function, log results, process.exit(0)
```

<!-- vercel.json cron format -->
```json
{ "path": "/api/cron/detect-series", "schedule": "10 6 * * *" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cron endpoint + vercel.json + extractor prompt</name>
  <files>src/app/api/cron/detect-series/route.ts, vercel.json, src/lib/scraper/extractor.ts</files>
  <action>
    1. Create `src/app/api/cron/detect-series/route.ts`:
       - Mirror the `/api/cron/archive/route.ts` pattern exactly
       - `export const maxDuration = 60;`
       - GET handler with CRON_SECRET Bearer auth check (return 401 if missing/wrong)
       - Call `detectAndTagSeries()` from `@/lib/series-detector`
       - Return `{ success: true, seriesUpserted, eventsTagged, timestamp }` on success
       - Catch errors, return `{ success: false, error: String(err) }` with status 500

    2. Add detect-series cron entry to `vercel.json`:
       - Add `{ "path": "/api/cron/detect-series", "schedule": "10 6 * * *" }` to the crons array
       - This runs at 6:10am UTC, 10 minutes after the scrape cron at 6:00am UTC
       - Place it after the archive entry (last in the list)

    3. Update Gemini extraction prompt in `src/lib/scraper/extractor.ts`:
       - Add `recurrence_pattern` field instruction to the prompt string, after the event_category guidance block
       - Add this text: `- recurrence_pattern: if the page indicates this event repeats on a schedule (e.g., "every Tuesday", "weekly open mic", "first Friday of each month"), capture that pattern as a short string. Omit if no recurrence is indicated.`
       - The Zod schema already accepts this field — only the prompt instruction is missing
  </action>
  <verify>
    <automated>npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /api/cron/detect-series endpoint exists with CRON_SECRET auth
    - vercel.json includes detect-series cron at "10 6 * * *"
    - Gemini extraction prompt includes recurrence_pattern instruction
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin trigger case + TriggerActions button + backfill script</name>
  <files>src/app/api/admin/trigger/[job]/route.ts, src/app/admin/_components/TriggerActions.tsx, src/lib/db/backfill-series.ts</files>
  <action>
    1. Add `detect-series` case to `src/app/api/admin/trigger/[job]/route.ts`:
       - Add before the `default` case in the switch
       - Pattern: `const { detectAndTagSeries } = await import('@/lib/series-detector');`
       - Call `detectAndTagSeries()`, return `{ success: true, seriesUpserted: result.seriesUpserted, eventsTagged: result.eventsTagged, timestamp }`
       - No discovery_runs insert needed (this is not a discovery job)

    2. Add "Detect Series" button to `src/app/admin/_components/TriggerActions.tsx`:
       - Add a new button after the "Run Archive" button, same styling (buttonClass)
       - Label: "Detect Series"
       - onClick: `() => trigger('detect-series')`
       - Show Spinner when `runningJob === 'detect-series'`
       - Add `formatSuccessMessage` case for 'detect-series': return `` `Series detection — ${body.seriesUpserted} series, ${body.eventsTagged} events tagged` ``

    3. Create `src/lib/db/backfill-series.ts`:
       - Follow `backfill-categories.ts` pattern exactly
       - `import 'dotenv/config';`
       - `import { detectAndTagSeries } from '@/lib/series-detector';`
       - Call `detectAndTagSeries()`, log `[backfill-series] N series upserted, N events tagged`
       - `main().catch(console.error).finally(() => process.exit(0));`
       - Comment at bottom: `// Run: tsx src/lib/db/backfill-series.ts`
  </action>
  <verify>
    <automated>npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Admin trigger route handles 'detect-series' case
    - TriggerActions shows "Detect Series" button with correct result message
    - backfill-series.ts script exists and follows established backfill pattern
    - `tsx src/lib/db/backfill-series.ts` can be run to seed existing events (SER-06)
  </done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` — full test suite passes
- `grep -r "detect-series" vercel.json` — cron entry present
- `grep -r "recurrence_pattern" src/lib/scraper/extractor.ts` — prompt instruction present
- `grep -r "detect-series" src/app/api/admin/trigger` — admin case present
- `grep -r "Detect Series" src/app/admin/_components/TriggerActions.tsx` — button present
- `ls src/lib/db/backfill-series.ts` — backfill script exists
</verification>

<success_criteria>
- Dedicated cron endpoint runs series detection independently from scrape (no timeout risk)
- Gemini extraction prompt explicitly asks for recurrence_pattern when page content indicates repetition
- Admin can trigger series detection on demand from dashboard
- Backfill script can seed series_id for all existing events on first run
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/31-series-detection/31-02-SUMMARY.md`
</output>
