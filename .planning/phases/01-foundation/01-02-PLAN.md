---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: false
requirements:
  - INFR-01

user_setup:
  - service: neon
    why: "Cloud Postgres database for the application"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> Connection string"
    dashboard_config:
      - task: "Create a Neon project (free tier)"
        location: "https://console.neon.tech -> New Project"
      - task: "Connect Neon integration to Vercel project"
        location: "Vercel Dashboard -> Project -> Settings -> Integrations -> Neon"
  - service: vercel
    why: "Hosting platform for Next.js deployment"
    env_vars: []
    dashboard_config:
      - task: "Import git repo as Vercel project"
        location: "https://vercel.com/new -> Import Git Repository"
      - task: "Verify DATABASE_URL env var is set (from Neon integration)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "App is deployed and accessible at a public Vercel URL"
    - "Health endpoint returns 200 with db: connected"
    - "Database has all three tables after migration"
    - "Seed data persists in the database (5 venues, 5 scrape_sources)"
  artifacts: []
  key_links:
    - from: "Vercel deployment"
      to: "Neon Postgres"
      via: "DATABASE_URL environment variable"
      pattern: "DATABASE_URL"
---

<objective>
Deploy the project to Vercel, connect Neon Postgres, run migrations and seed data, and verify the full stack is working end-to-end.

Purpose: Complete INFR-01 by proving the app is live with a connected database. This is the gate before Phase 2 can begin.
Output: A live Vercel URL returning healthy status with seeded database.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git repo, push to GitHub, and prepare for deployment</name>
  <files></files>
  <action>
1. Initialize git repository if not already initialized:
   ```
   git init
   git add -A
   git commit -m "feat(01-foundation): scaffold Next.js 16 project with Drizzle schema and seed data"
   ```

2. Create a GitHub repository and push:
   ```
   gh repo create eastcoastlocal --public --source=. --push
   ```
   If repo already exists, just push to it.

3. Verify the push succeeded and code is on GitHub.

Note: The user will need to import this repo in Vercel and connect Neon in the next step.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/eastcoastlocal && git log --oneline -1 && gh repo view --json url -q .url 2>/dev/null || echo "repo pushed"</automated>
  </verify>
  <done>Code is committed and pushed to GitHub, ready for Vercel import</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User connects Neon + Vercel and triggers first deploy</name>
  <files></files>
  <action>
User must complete these steps (Claude cannot authenticate to Vercel/Neon dashboards):

1. Go to https://console.neon.tech and create a new project (free tier) if you don't have one
2. Go to https://vercel.com/new and import the `eastcoastlocal` GitHub repository
3. In Vercel project settings, go to Integrations and connect the Neon integration (this auto-sets DATABASE_URL)
4. Alternatively, manually copy the Neon connection string and add it as DATABASE_URL in Vercel Environment Variables
5. Trigger a deploy (or it may auto-deploy from the import)
6. Wait for the build to succeed — the build step runs `npm run db:migrate && next build` which creates the tables in Neon
7. Once deployed, visit `https://YOUR-APP.vercel.app/api/health` and confirm you see: `{"status":"ok","db":"connected"}`
8. Run the seed script locally with your Neon connection string:
   ```
   DATABASE_URL="your-neon-connection-string" npm run db:seed
   ```
9. Share the deployed URL and confirm health check returns ok
  </action>
  <verify>
    <automated>echo "Requires user action — Vercel/Neon dashboard setup"</automated>
  </verify>
  <done>User confirms: deployed URL is live, health check returns ok, seed ran successfully</done>
</task>

<task type="auto">
  <name>Task 3: Verify deployment and database state</name>
  <files></files>
  <action>
After user provides the Vercel URL:

1. Curl the health endpoint to verify DB connectivity:
   ```
   curl -s https://YOUR-APP.vercel.app/api/health | jq .
   ```
   Expected: `{"status":"ok","db":"connected"}`

2. If the user has shared access or can run a query, verify table existence by checking the health endpoint response. The health check confirms the DB connection works and migrations ran (tables exist because drizzle-kit migrate ran in the build step).

3. If any issues, help troubleshoot:
   - Build failure: Check Vercel build logs for DATABASE_URL errors
   - Health returns 500: Check Neon connection string format
   - Seed failure: Verify DATABASE_URL is accessible from local machine (Neon allows connections from anywhere by default)
  </action>
  <verify>
    <automated>echo "Verification requires deployed URL from user — manual curl test"</automated>
  </verify>
  <done>
    - Health endpoint returns `{"status":"ok","db":"connected"}`
    - Build logs show migrations ran successfully
    - Seed script populated venues and scrape_sources tables
    - INFR-01 is satisfied: app deployed on Vercel with Neon Postgres connected
  </done>
</task>

</tasks>

<verification>
1. `curl -f https://YOUR-APP.vercel.app/api/health` returns 200 with `{"status":"ok","db":"connected"}`
2. Vercel build logs show `drizzle-kit migrate` completed successfully
3. Database contains 5 venues and 5 scrape_sources rows after seed
</verification>

<success_criteria>
- App is live at a public Vercel URL
- Health endpoint confirms database connectivity
- All three tables exist in Neon Postgres (created by migrations in build step)
- Seed data persists: 5 venues across 4 provinces, 5 scrape_sources (4 enabled, 1 disabled)
- Phase 1 success criteria #1 and #2 from ROADMAP are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
