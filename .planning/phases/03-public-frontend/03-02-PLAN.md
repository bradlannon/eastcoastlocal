---
phase: 03-public-frontend
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/components/map/MapClient.tsx
  - src/components/map/MapWrapper.tsx
  - src/components/map/MapBoundsTracker.tsx
  - src/components/map/ClusterLayer.tsx
  - src/components/map/VenuePopup.tsx
  - src/components/events/EventList.tsx
  - src/components/events/EventCard.tsx
  - src/components/layout/MobileTabBar.tsx
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-09

must_haves:
  truths:
    - "User sees an interactive map centered on Atlantic Canada with CartoDB Positron tiles"
    - "Event pins cluster when zoomed out showing count, expand to individual pins when zoomed in"
    - "Clicking a pin opens a popup showing venue name, event count, and list of events with band + date"
    - "Scrollable event list on the right shows events visible in the current map viewport, sorted by date"
    - "On mobile, user sees map-first with a bottom tab bar to toggle between Map and List views"
  artifacts:
    - path: "src/components/map/MapClient.tsx"
      provides: "Main interactive map with Leaflet, clusters, popups"
      min_lines: 60
    - path: "src/components/map/ClusterLayer.tsx"
      provides: "MarkerClusterGroup with venue markers"
      min_lines: 20
    - path: "src/components/map/VenuePopup.tsx"
      provides: "Popup listing events at a venue with View Details links"
      min_lines: 20
    - path: "src/components/events/EventList.tsx"
      provides: "Scrollable event list filtered by viewport bounds"
      min_lines: 30
    - path: "src/components/events/EventCard.tsx"
      provides: "Individual event card with band, venue, date, price"
      min_lines: 20
    - path: "src/app/page.tsx"
      provides: "Split-screen layout: map left, list right, mobile tab toggle"
      min_lines: 40
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/map/MapWrapper.tsx"
      via: "Dynamic import with ssr:false"
      pattern: "dynamic.*import.*MapClient"
    - from: "src/components/map/MapClient.tsx"
      to: "src/app/api/events/route.ts"
      via: "Client-side fetch on mount"
      pattern: "fetch.*/api/events"
    - from: "src/components/map/MapBoundsTracker.tsx"
      to: "src/components/events/EventList.tsx"
      via: "Bounds state callback updates list filter"
      pattern: "onBoundsChange"
---

<objective>
Build the interactive map with clustered venue pins and the synced event list, wired into a split-screen layout with mobile responsiveness.

Purpose: This is the core UX — the map-and-list view that lets users visually discover live music events across Atlantic Canada. Everything from Plan 01 (API, filters, types) feeds into this.
Output: Working split-screen map/list page with clustering, popups, viewport sync, and mobile tab bar.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-public-frontend/03-CONTEXT.md
@.planning/phases/03-public-frontend/03-RESEARCH.md
@.planning/phases/03-public-frontend/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and utilities this plan consumes -->

From src/types/index.ts:
```typescript
export type Event = InferSelectModel<typeof events>;
export type Venue = InferSelectModel<typeof venues>;
export type EventWithVenue = {
  events: Event;
  venues: Venue;
};
```

From src/lib/province-bounds.ts:
```typescript
export const ATLANTIC_CANADA_CENTER: [number, number];
export const INITIAL_ZOOM: number;
export const ATLANTIC_CANADA_BOUNDS: LatLngBoundsLiteral;
export const PROVINCE_BOUNDS: Record<string, LatLngBoundsLiteral>;
export const PROVINCE_LABELS: Record<string, string>;
```

From src/lib/filter-utils.ts:
```typescript
export function filterByDateRange(events: EventWithVenue[], when: string | null): EventWithVenue[];
export function filterByProvince(events: EventWithVenue[], province: string | null): EventWithVenue[];
export function filterByBounds(events: EventWithVenue[], bounds: { north: number; south: number; east: number; west: number } | null): EventWithVenue[];
```

From src/app/api/events/route.ts:
```typescript
// GET /api/events -> EventWithVenue[]
export async function GET(): Promise<NextResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout overhaul and interactive map with clustering</name>
  <files>
    src/app/layout.tsx
    src/app/globals.css
    src/components/map/MapClient.tsx
    src/components/map/MapWrapper.tsx
    src/components/map/MapBoundsTracker.tsx
    src/components/map/ClusterLayer.tsx
    src/components/map/VenuePopup.tsx
  </files>
  <action>
1. Update `src/app/layout.tsx`:
   - Change metadata: title "East Coast Local" and description "Discover live music events across Atlantic Canada"
   - Wrap children in `<NuqsAdapter>` from `nuqs/adapters/next/app`
   - Keep Geist font setup, keep antialiased class
   - Remove dark theme class if any; ensure light theme

2. Update `src/app/globals.css`:
   - Remove the dark mode `@media (prefers-color-scheme: dark)` block — light theme only per user decision
   - Keep the existing Tailwind import, theme inline, and body styles
   - Ensure body uses `font-family: var(--font-geist-sans)` (already set up)

3. Update `src/components/map/MapWrapper.tsx` (created in Plan 01 for MiniMap):
   - Add a default export that dynamic-imports `./MapClient` with `ssr: false`
   - Loading fallback: full-height animated pulse placeholder with "Loading map..." text centered

4. Create `src/components/map/MapClient.tsx` ('use client'):
   - Import CSS in correct order:
     - `leaflet/dist/leaflet.css`
     - `react-leaflet-cluster/dist/assets/MarkerCluster.css`
     - `react-leaflet-cluster/dist/assets/MarkerCluster.Default.css`
     - `leaflet-defaulticon-compatibility/dist/leaflet-defaulticon-compatibility.css`
     - `leaflet-defaulticon-compatibility`
   - Accept props: `events: EventWithVenue[]`, `onBoundsChange: (bounds: { north, south, east, west }) => void`
   - Render `<MapContainer>` with:
     - `center={ATLANTIC_CANADA_CENTER}`, `zoom={INITIAL_ZOOM}`
     - `style={{ height: '100%', width: '100%' }}`
     - `scrollWheelZoom={true}`
   - CartoDB Positron TileLayer: `https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png`
   - `<MapBoundsTracker>` child for viewport tracking
   - `<ClusterLayer>` child receiving events
   - "No events here" overlay: show when no events are within current bounds. Positioned absolute over map, semi-transparent background, centered text.

5. Create `src/components/map/MapBoundsTracker.tsx` ('use client'):
   - Uses `useMapEvents` hook (must be child of MapContainer)
   - Listens to `moveend` and `zoomend` events
   - Calls `onBoundsChange` with bounds converted to `{ north, south, east, west }` plain object (using `getBounds().getNorth()` etc.)
   - Also fire on initial load (call onBoundsChange in a useEffect after map is ready)

6. Create `src/components/map/ClusterLayer.tsx` ('use client'):
   - Accept props: `events: EventWithVenue[]`
   - Group events by `venue_id` into a Map: `Map<number, { venue: Venue, events: EventWithVenue[] }>`
   - Render `<MarkerClusterGroup chunkedLoading>` wrapping venue Markers
   - Each venue Marker:
     - Position: `[venue.lat, venue.lng]` (skip venues with null coords)
     - Icon: custom `L.divIcon` with bold accent color `#E85D26` (12px circle, white border, subtle shadow)
     - Contains a `<Popup>` with `<VenuePopup>` component

7. Create `src/components/map/VenuePopup.tsx`:
   - Accept props: `venue: Venue, events: EventWithVenue[]`
   - Render:
     - Venue name as bold header
     - Event count: "{N} upcoming event(s)"
     - List of events sorted by date: each showing performer name, formatted date (e.g., "Sat, Mar 15"), event_time if available
     - Each event has a "View Details" link to `/event/{event.id}`
   - Max height with overflow-y-auto for venues with many events
   - Clean typography, no images
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/eastcoastlocal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - MapClient renders CartoDB Positron map centered on Atlantic Canada
    - Venue markers use custom orange-red accent icon
    - Markers cluster with count badges when zoomed out
    - Clicking a pin opens popup with venue name, event count, and event list
    - Each popup event has a "View Details" link to /event/[id]
    - MapBoundsTracker reports viewport changes
    - Light theme applied globally (no dark mode)
    - Layout metadata updated to "East Coast Local"
  </done>
</task>

<task type="auto">
  <name>Task 2: Event list, split-screen layout, mobile tab bar, and viewport sync</name>
  <files>
    src/app/page.tsx
    src/components/events/EventList.tsx
    src/components/events/EventCard.tsx
    src/components/layout/MobileTabBar.tsx
  </files>
  <action>
1. Create `src/components/events/EventCard.tsx`:
   - Accept props: `event: EventWithVenue`
   - Card layout with subtle border/shadow, rounded corners, padding
   - Content:
     - Performer name: large, bold, dark text
     - Venue name + city: smaller, muted text below (e.g., "The Carleton, Halifax")
     - Date: formatted with date-fns `format(event.events.event_date, 'EEE, MMM d')`, and event_time if available
     - Price: small badge/pill if `event.events.price` exists
   - No cover images per user decision
   - Entire card is clickable — links to `/event/{event.events.id}`
   - Hover state: subtle shadow increase or border color change
   - Add `data-venue-id` attribute on the card for cross-highlight (Plan 03 will wire this)

2. Create `src/components/events/EventList.tsx`:
   - Accept props: `events: EventWithVenue[]`, `emptyMessage?: string`
   - Sort events by `event_date` ascending
   - Render list of EventCard components
   - If events is empty, show friendly empty state: the `emptyMessage` or default "No events in this area. Zoom out to see more."
   - Scrollable container with overflow-y-auto, flex-1 height
   - Event count header: "{N} events" in muted text at top

3. Create `src/components/layout/MobileTabBar.tsx` ('use client'):
   - Fixed at bottom of screen on mobile (below md breakpoint)
   - Two tabs: [Map icon + "Map"] [List icon + "List"]
   - Accept props: `activeTab: 'map' | 'list'`, `onTabChange: (tab: 'map' | 'list') => void`
   - Active tab highlighted with accent color, inactive muted
   - Use simple SVG icons (map pin for Map, list/bars for List)
   - z-index above map
   - Hidden on desktop (md:hidden)

4. Rewrite `src/app/page.tsx` as a client component ('use client'):
   - Fetch events from `/api/events` on mount (useEffect + useState)
   - Track map bounds in state: `useState<{north,south,east,west} | null>(null)`
   - Track mobile active tab: `useState<'map' | 'list'>('map')`
   - Compute visible events: `filterByBounds(allEvents, bounds)` using filter-utils
   - Desktop layout (md and up):
     - Minimal header bar: "East Coast Local" h1 on left, event count on right. Reserve space for filter controls (Plan 03 adds them).
     - Below header: flex row, map on left (flex-1, min-w-0), event list on right (w-[400px], border-left)
     - Both map and list fill remaining viewport height (h-[calc(100vh-header)])
   - Mobile layout (below md):
     - Same header (responsive)
     - Show map OR list based on activeTab (not both — saves DOM weight)
     - Map fills viewport minus header and tab bar height
     - List fills same space when active
     - MobileTabBar fixed at bottom
   - Pass `onBoundsChange` callback to MapWrapper/MapClient
   - Pass filtered events to EventList
   - Loading state: show skeleton/pulse until events fetched
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/eastcoastlocal && npx tsc --noEmit --pretty 2>&1 | head -30 && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Split-screen layout: map left, scrollable event list right on desktop
    - Event list shows only events visible in current map viewport
    - List updates as user pans/zooms the map (no search button)
    - Event cards show performer, venue+city, date, price (if available)
    - Cards link to /event/[id]
    - Mobile: map-first with bottom tab bar to toggle Map/List
    - Tab bar hidden on desktop
    - Event count displayed in header area
    - Empty state message when no events in viewport
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test` passes (all existing + Plan 01 tests)
- `npm run build` succeeds (SSR safety — no window errors)
- Visual: map renders with pins, list syncs with viewport
</verification>

<success_criteria>
- Interactive map centered on Atlantic Canada with clustered venue pins
- Pin popups show venue name and event list with "View Details" links
- Split-screen layout with map left, event list right
- List syncs with map viewport in real-time
- Mobile tab bar toggles between map and list views
- Light theme throughout, no dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-frontend/03-02-SUMMARY.md`
</output>
