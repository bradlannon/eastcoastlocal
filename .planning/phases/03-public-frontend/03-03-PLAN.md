---
phase: 03-public-frontend
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/app/page.tsx
  - src/components/events/EventFilters.tsx
  - src/components/map/GeolocationButton.tsx
  - src/components/map/MapClient.tsx
  - src/components/map/MapViewController.tsx
  - src/components/events/EventCard.tsx
  - src/components/events/EventList.tsx
autonomous: false
requirements:
  - MAP-05
  - MAP-06
  - MAP-07
  - MAP-09
  - INFR-02

must_haves:
  truths:
    - "User can filter events by Today, This Weekend, and This Week using toggle chips"
    - "User can filter by province using a dropdown that auto-zooms the map"
    - "Clear filters button appears when any filter is active and resets all filters"
    - "Filters persist in URL query params and are shareable/bookmarkable"
    - "User can click Near Me button to center map on their current location"
    - "Hovering a list card highlights the corresponding map pin"
    - "All views are usable on a phone screen"
    - "Map loads and is interactive in under 3 seconds on broadband"
  artifacts:
    - path: "src/components/events/EventFilters.tsx"
      provides: "Date chip toggles, province dropdown, clear filters, event count"
      min_lines: 40
    - path: "src/components/map/GeolocationButton.tsx"
      provides: "Near me button using browser geolocation API"
      min_lines: 15
    - path: "src/components/map/MapViewController.tsx"
      provides: "Programmatic map view control for province zoom and geolocation"
      min_lines: 15
  key_links:
    - from: "src/components/events/EventFilters.tsx"
      to: "URL query params"
      via: "nuqs useQueryState"
      pattern: "useQueryState.*when|useQueryState.*province"
    - from: "src/components/map/MapViewController.tsx"
      to: "src/lib/province-bounds.ts"
      via: "fitBounds with province bounds"
      pattern: "PROVINCE_BOUNDS.*fitBounds"
    - from: "src/app/page.tsx"
      to: "src/lib/filter-utils.ts"
      via: "filterByDateRange and filterByProvince applied to events"
      pattern: "filterByDateRange|filterByProvince"
---

<objective>
Add filter controls (date chips, province dropdown with auto-zoom), geolocation button, cross-highlight between list and map, and verify the complete UI works on mobile and desktop.

Purpose: This completes the discovery UX — users can narrow events by time and location, find events near them, and interact fluidly between map and list.
Output: Fully functional, filtered, interactive map+list experience with visual verification.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-public-frontend/03-CONTEXT.md
@.planning/phases/03-public-frontend/03-RESEARCH.md
@.planning/phases/03-public-frontend/03-01-SUMMARY.md
@.planning/phases/03-public-frontend/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 — components and utilities this plan consumes -->

From src/lib/filter-utils.ts:
```typescript
export function filterByDateRange(events: EventWithVenue[], when: string | null): EventWithVenue[];
export function filterByProvince(events: EventWithVenue[], province: string | null): EventWithVenue[];
export function filterByBounds(events: EventWithVenue[], bounds: { north: number; south: number; east: number; west: number } | null): EventWithVenue[];
```

From src/lib/province-bounds.ts:
```typescript
export const ATLANTIC_CANADA_CENTER: [number, number];
export const INITIAL_ZOOM: number;
export const ATLANTIC_CANADA_BOUNDS: LatLngBoundsLiteral;
export const PROVINCE_BOUNDS: Record<string, LatLngBoundsLiteral>;
export const PROVINCE_LABELS: Record<string, string>;
```

From src/components/map/MapClient.tsx (Plan 02):
```typescript
// Props: events, onBoundsChange
// Children rendered inside MapContainer
```

From src/app/page.tsx (Plan 02):
```typescript
// Client component with state for: allEvents, bounds, activeTab
// Computes visibleEvents = filterByBounds(allEvents, bounds)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter controls with URL state, province auto-zoom, geolocation, and cross-highlight</name>
  <files>
    src/components/events/EventFilters.tsx
    src/components/map/GeolocationButton.tsx
    src/components/map/MapViewController.tsx
    src/components/map/MapClient.tsx
    src/components/events/EventCard.tsx
    src/components/events/EventList.tsx
    src/app/page.tsx
  </files>
  <action>
1. Create `src/components/events/EventFilters.tsx` ('use client'):
   - Use nuqs `useQueryState` for filter state:
     - `const [when, setWhen] = useQueryState('when')` — values: null, 'today', 'weekend', 'week'
     - `const [province, setProvince] = useQueryState('province')` — values: null, 'NB', 'NS', 'PEI', 'NL'
   - Accept props: `eventCount: number`, `onProvinceChange?: (province: string | null) => void` (to trigger map zoom)
   - Render:
     - Event count: "{N} events" displayed prominently
     - Date quick-filter chips in a row: All (default/null) | Today | This Weekend | This Week
       - Chip styling: pill-shaped, accent background when active (#E85D26 matching pin color), gray outline when inactive
       - Clicking active chip deselects it (sets to null = "All")
     - Province dropdown: `<select>` or custom dropdown with "All Provinces" + 4 province options using PROVINCE_LABELS
       - On change: call `setProvince(value || null)` and `onProvinceChange(value || null)`
     - "Clear filters" link/button: visible only when `when || province` is truthy
       - Clicking resets both to null
   - Responsive: chips wrap on narrow screens, dropdown full-width on mobile
   - Export the `when` and `province` values for parent component to use in filtering

2. Create `src/components/map/GeolocationButton.tsx` ('use client'):
   - Must be a child of MapContainer (uses `useMap()` hook)
   - Renders a button positioned absolute bottom-6 right-4 z-[1000] (Google Maps style)
   - Styling: white circle, subtle shadow, location crosshair icon (SVG)
   - On click: call `navigator.geolocation.getCurrentPosition`
     - Success: `map.flyTo([lat, lng], 12)` with smooth animation
     - Error: log warning, optionally show toast/alert "Could not access your location"
   - Only render if `navigator.geolocation` exists (feature detect)
   - `aria-label="Center map on my location"`

3. Create `src/components/map/MapViewController.tsx` ('use client'):
   - Must be a child of MapContainer (uses `useMap()` hook)
   - Accept props: `province: string | null`
   - useEffect watching `province`:
     - If province is a key in PROVINCE_BOUNDS: `map.fitBounds(PROVINCE_BOUNDS[province])`
     - If province is null (cleared): `map.fitBounds(ATLANTIC_CANADA_BOUNDS)` to zoom back out
   - This component renders null — it's a pure side-effect hook component

4. Update `src/components/map/MapClient.tsx`:
   - Add `<GeolocationButton />` as child of MapContainer
   - Add `<MapViewController province={province} />` as child of MapContainer
   - Accept new prop: `province: string | null` to pass to MapViewController
   - Accept new prop: `highlightedVenueId: number | null` — when set, visually emphasize that venue's marker (e.g., increase icon size or add pulsing ring via CSS class)

5. Update `src/components/events/EventCard.tsx`:
   - Add `onMouseEnter` and `onMouseLeave` handlers
   - Accept props: `onHover?: (venueId: number | null) => void`
   - On mouse enter: call `onHover(event.venues.id)`
   - On mouse leave: call `onHover(null)`

6. Update `src/components/events/EventList.tsx`:
   - Accept new prop: `onHoverVenue?: (venueId: number | null) => void`
   - Pass through to each EventCard's `onHover` prop

7. Update `src/app/page.tsx`:
   - Add filter state: read `when` and `province` from nuqs (import `useQueryState` from 'nuqs')
   - Add `highlightedVenueId` state: `useState<number | null>(null)`
   - Update event filtering chain:
     ```
     const dateFiltered = filterByDateRange(allEvents, when);
     const provinceFiltered = filterByProvince(dateFiltered, province);
     const visibleEvents = filterByBounds(provinceFiltered, bounds);
     ```
   - Move EventFilters component into the header area, passing `eventCount={visibleEvents.length}` and `onProvinceChange` callback
   - Pass `province` to MapClient
   - Pass `highlightedVenueId` to MapClient
   - Pass `onHoverVenue` to EventList
   - Friendly empty states based on active filters:
     - With date filter: "No events {when description}. Try a different date range."
     - With province filter: "No events in {province label}. Try All Provinces."
     - With both: "No events matching your filters. Clear filters to see all events."
     - No filters, just viewport: "No events in this area. Zoom out to see more."
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/eastcoastlocal && npx tsc --noEmit --pretty 2>&1 | head -30 && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Date filter chips (All/Today/This Weekend/This Week) toggle correctly and update URL params
    - Province dropdown filters events and auto-zooms map to province bounds
    - "Clear filters" appears when filters active and resets all
    - URL reflects filter state (e.g., /?when=weekend&province=NS)
    - Geolocation button centers map on user location
    - Hovering a list card highlights the corresponding map pin
    - Friendly empty state messages for each filter combination
    - All existing tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete UI</name>
  <files>none — verification only</files>
  <action>
    Human verifies the complete Phase 3 frontend by testing all requirements in the browser.
    Run `npm run dev` and open http://localhost:3000 before handing off to user.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/eastcoastlocal && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves all 13 verification items listed in how-to-verify below</done>
  <what-built>
    Complete public frontend for East Coast Local: interactive map with clustered venue pins, synced event list, date and province filters with URL state, event detail pages, geolocation, cross-highlight, mobile-responsive layout.
  </what-built>
  <how-to-verify>
    1. Open `npm run dev` and visit http://localhost:3000
    2. **Map (MAP-01):** Verify map is centered on Atlantic Canada with light CartoDB tiles showing all 4 provinces
    3. **Clusters (MAP-02):** Zoom out — pins should cluster with count badges. Zoom in — clusters expand to individual pins.
    4. **Popups (MAP-03):** Click a pin — popup should show venue name, event count, list of events (performer + date), "View Details" links
    5. **List sync (MAP-04):** Pan/zoom the map — the event list on the right should update in real-time to show only visible events
    6. **Date filters (MAP-05):** Click "Today", "This Weekend", "This Week" chips — both map pins and list should filter. Check URL updates (e.g., ?when=weekend)
    7. **Province filter (MAP-06):** Select a province from dropdown — map should auto-zoom to that province, list filters accordingly. Select "All Provinces" to zoom back out.
    8. **Geolocation (MAP-07):** Click the location button (bottom-right of map) — map should center on your location (allow browser permission)
    9. **Event detail (MAP-08):** Click "View Details" on a popup event — should navigate to /event/[id] with full event info, mini-map, "More at venue" section. Click "Back to map" — map state (zoom, filters) should be preserved.
    10. **Mobile (MAP-09):** Resize browser to phone width (375px) — should see map with bottom tab bar [Map] [List]. Toggle between tabs. Filters should be usable.
    11. **Performance (INFR-02):** Open DevTools Network tab, hard refresh — map should be interactive within 3 seconds on broadband
    12. **Empty states:** Apply filters that return no results — verify friendly message appears
    13. **Cross-highlight:** Hover over a list card — corresponding map pin should visually highlight
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test` passes (all 57+ existing tests + filter-utils + API route tests)
- `npm run build` succeeds without SSR errors
- All 10 MAP requirements and INFR-02 are addressed
</verification>

<success_criteria>
- All filters work and persist in URL
- Province dropdown auto-zooms map
- Geolocation button centers map on user
- Cross-highlight between list and map works
- Mobile layout is usable with tab bar toggle
- Map loads under 3 seconds on broadband
- Human approves visual/functional verification
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-frontend/03-03-SUMMARY.md`
</output>
