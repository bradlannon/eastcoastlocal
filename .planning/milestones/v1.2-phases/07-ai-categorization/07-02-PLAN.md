---
phase: 07-ai-categorization
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/lib/db/backfill-categories.ts
autonomous: false
requirements: [CAT-02]

must_haves:
  truths:
    - "No events in the database have null event_category values"
    - "Historical events that had no category now have 'community' as their category"
  artifacts:
    - path: "src/lib/db/backfill-categories.ts"
      provides: "Backfill script that sets null event_category to community"
      contains: "isNull"
  key_links:
    - from: "src/lib/db/backfill-categories.ts"
      to: "src/lib/db/schema.ts"
      via: "drizzle update on events table"
      pattern: "db\\.update.*events"
---

<objective>
Run the existing backfill script to clear all null event_category values from the database, satisfying CAT-02's requirement that no events lack a category at feature launch.

Purpose: CAT-02 requires all existing events to have a category. The backfill script was created in Phase 6 and sets nulls to 'community'.
Output: Zero null event_category values in production database.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-category-schema/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run backfill-categories script against production database</name>
  <files>src/lib/db/backfill-categories.ts</files>
  <action>
    Run the existing backfill script: `npx tsx src/lib/db/backfill-categories.ts`

    This script:
    - Connects to the Neon production database via DATABASE_URL in .env
    - Runs `db.update(events).set({ event_category: 'community' }).where(isNull(events.event_category))`
    - Logs the number of rows updated
    - Exits with process.exit(0)

    Do NOT modify the script — it was created and tested in Phase 6. Just run it.

    If the script reports 0 rows updated, that is acceptable — it means no nulls existed (the DB default may have already covered all rows).
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/eastcoastlocal && npx tsx src/lib/db/backfill-categories.ts</automated>
  </verify>
  <done>Script runs successfully; reports rows updated (or 0 if no nulls existed); no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify categorization pipeline and backfill via Drizzle Studio</name>
  <action>
    Human verifies that the AI categorization pipeline (Plan 01) and backfill (Task 1) are working correctly by inspecting the production database.
  </action>
  <verify>Human confirms no NULL event_category values in events table</verify>
  <done>Human approved: no null categories remain, extraction pipeline ready for all event types</done>
  <what-built>AI categorization pipeline (Plan 01) and backfill of existing events (Plan 02 Task 1). The extraction pipeline now assigns categories to all event types, and all historical events have been backfilled with 'community'.</what-built>
  <how-to-verify>
    1. Open Drizzle Studio: `npx drizzle-kit studio`
    2. Check the `events` table — verify the `event_category` column has no NULL values
    3. Confirm existing events show 'community' as their category (the backfill default)
    4. (Optional) To test AI categorization on a real venue, run the scraper against a known venue and check that the new event gets a non-default category assigned by Gemini
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Backfill script completes without errors
2. No null event_category values remain in the events table (verified via db:studio)
</verification>

<success_criteria>
- Backfill script ran successfully against production Neon database
- Zero null event_category values in events table
- Human verified via Drizzle Studio
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-categorization/07-02-SUMMARY.md`
</output>
