# Phase 6: Category Schema - Context

**Gathered:** 2026-03-14
**Status:** Ready for planning

<domain>
## Phase Boundary

DB migration adding `event_category` enum column to the events table and a `discovered_sources` staging table for the discovery pipeline. This is the hard gate for all v1.2 work.

</domain>

<decisions>
## Implementation Decisions

### Category Taxonomy
- Fixed 8-value enum: live_music, comedy, theatre, arts, sports, festival, community, other
- Friendly display labels: live_music → "Live Music", theatre → "Theatre", etc.
- Each category gets both a distinct color AND a small icon (for chips and badges)
- Default category for ambiguous events: "community" (not "other")
- Taxonomy enforced via z.enum() in extraction schema — LLM cannot produce out-of-taxonomy values

### Discovery Staging
- Claude's discretion on table structure — must include status (pending/approved/rejected), source URL, domain, and discovery metadata
- Domain-based dedup to prevent rediscovering existing venues

### Claude's Discretion
- discovered_sources table column design
- pgEnum implementation details (export to avoid drizzle bug #5174)
- Backfill script approach
- Category color and icon assignments
- Migration file naming

</decisions>

<specifics>
## Specific Ideas

- Icons + colors per category — visually scannable chips and badges
- "Community" as the catch-all instead of "other" — friendlier label for uncategorized events

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/lib/db/schema.ts`: Existing Drizzle schema with venues, events, scrape_sources tables
- `drizzle.config.ts`: Drizzle Kit config pointing to Neon
- `src/lib/db/seed.ts`: Seed script pattern with --reset flag

### Established Patterns
- Schema uses `pgTable`, `serial`, `text`, `timestamp`, `doublePrecision`, `boolean`
- Migrations in `drizzle/` directory via `drizzle-kit generate` + `drizzle-kit migrate`
- Build command runs migrations first: `npm run db:migrate && next build`

### Integration Points
- `src/lib/db/schema.ts`: Add pgEnum + event_category column + discovered_sources table
- `src/lib/schemas/extracted-event.ts`: Will need z.enum() category field in Phase 7
- `drizzle/`: New migration file generated by drizzle-kit

</code_context>

<deferred>
## Deferred Ideas

- Zoom-to-location button on event cards (backlog from v1.1)

</deferred>

---

*Phase: 06-category-schema*
*Context gathered: 2026-03-14*
