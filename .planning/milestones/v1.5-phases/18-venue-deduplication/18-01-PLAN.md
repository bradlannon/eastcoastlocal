---
phase: 18-venue-deduplication
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/scraper/venue-dedup.ts
  - src/lib/scraper/venue-dedup.test.ts
autonomous: true
requirements:
  - DEDUP-01
  - DEDUP-03

must_haves:
  truths:
    - "venueNameRatio returns proportional edit distance between two venue names"
    - "scoreVenueCandidate returns 'merge' when name ratio < 0.15 AND geo < 100m"
    - "scoreVenueCandidate returns 'review' when name matches but geo > 500m"
    - "scoreVenueCandidate returns 'review' when geo < 100m but name differs"
    - "scoreVenueCandidate returns 'review' when incoming has no geo (lat/lng null) even if name matches"
    - "scoreVenueCandidate returns 'keep_separate' when neither signal matches"
    - "findBestMatch iterates candidates and returns the best merge or first review decision"
  artifacts:
    - path: "src/lib/scraper/venue-dedup.ts"
      provides: "Venue dedup scoring module with pure functions"
      exports: ["venueNameRatio", "scoreVenueCandidate", "findBestMatch", "normalizeVenueName", "MERGE_NAME_RATIO", "MERGE_GEO_METERS", "REVIEW_GEO_METERS", "DedupeDecision"]
    - path: "src/lib/scraper/venue-dedup.test.ts"
      provides: "Full test coverage for scoring logic"
      min_lines: 80
  key_links:
    - from: "src/lib/scraper/venue-dedup.ts"
      to: "fastest-levenshtein"
      via: "import { distance }"
      pattern: "import.*distance.*fastest-levenshtein"
    - from: "src/lib/scraper/venue-dedup.ts"
      to: "src/lib/timelapse-utils.ts"
      via: "import { haversineDistance }"
      pattern: "import.*haversineDistance.*timelapse-utils"
---

<objective>
Build and test the venue deduplication scoring module as a standalone, pure-function library.

Purpose: Isolate the matching/decision logic so it can be tested without database mocks, validated against real venue name pairs, and consumed by both the TM pipeline (Plan 02) and the backfill CLI (Plan 02).

Output: `src/lib/scraper/venue-dedup.ts` with full test coverage in `src/lib/scraper/venue-dedup.test.ts`.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.5-phases/18-venue-deduplication/18-CONTEXT.md
@.planning/milestones/v1.5-phases/18-venue-deduplication/18-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing codebase contracts this plan depends on -->

From src/lib/timelapse-utils.ts:
```typescript
export function haversineDistance(
  lat1: number, lng1: number,
  lat2: number, lng2: number
): number; // returns meters
```

From fastest-levenshtein (npm):
```typescript
export function distance(a: string, b: string): number; // edit distance
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install fastest-levenshtein and create venue-dedup scoring module with TDD</name>
  <files>src/lib/scraper/venue-dedup.ts, src/lib/scraper/venue-dedup.test.ts, package.json</files>
  <behavior>
    - venueNameRatio("Scotiabank Centre", "Scotiabank Centre") returns 0.0
    - venueNameRatio("Marquee Ballroom", "The Marquee Ballroom") returns ~0.20 (distance 4 / max 20)
    - venueNameRatio("Harbour Station", "Saint John Arena") returns high ratio (> 0.5, clearly different)
    - normalizeVenueName strips leading "The ", lowercases, trims, collapses whitespace
    - normalizeVenueName("  The Scotiabank Centre  ") returns "scotiabank centre"
    - scoreVenueCandidate returns { action: 'merge' } when name ratio < 0.15 AND geo < 100m
    - scoreVenueCandidate returns { action: 'review', reason: 'name_match_geo_distant' } when name ratio < 0.15 AND geo > 500m
    - scoreVenueCandidate returns { action: 'review', reason: 'geo_close_name_differs' } when geo < 100m AND name ratio >= 0.15
    - scoreVenueCandidate returns { action: 'review', reason: 'name_match_no_geo' } when name ratio < 0.15 AND incoming has null lat/lng (TM venue at creation time)
    - scoreVenueCandidate returns { action: 'keep_separate' } when name ratio >= 0.15 AND geo > 500m
    - scoreVenueCandidate returns { action: 'keep_separate' } when name ratio >= 0.15 AND geo > 100m (no signal matches)
    - findBestMatch scans an array of candidates and returns the best merge decision, or the first review, or keep_separate if none match
    - findBestMatch with empty candidates array returns { action: 'keep_separate' }
    - findBestMatch prefers merge over review when multiple candidates score differently
    - Borderline case: name ratio between 0.15 and 0.35, geo between 100m and 500m returns review with reason 'name_match_geo_uncertain'
  </behavior>
  <action>
    **Step 0: Install dependency**
    ```bash
    npm install fastest-levenshtein
    ```

    **RED phase: Write tests first in `src/lib/scraper/venue-dedup.test.ts`**

    Test suites to create:
    1. `describe('normalizeVenueName')` - stripping "The ", lowercase, trim, whitespace collapse
    2. `describe('venueNameRatio')` - proportional edit distance with real Atlantic Canada venue names
    3. `describe('scoreVenueCandidate')` - full decision matrix:
       - Auto-merge case (both signals pass)
       - Name match but geo distant (> 500m) -> review
       - Geo close but name differs -> review
       - Name match but no geo on incoming (null lat/lng) -> review with reason 'name_match_no_geo'
       - Neither signal matches -> keep_separate
       - Borderline geo (100m-500m) with name match -> review
    4. `describe('findBestMatch')` - scanning candidates array:
       - Returns merge when one candidate passes both signals
       - Returns review when no merge but a borderline exists
       - Returns keep_separate when no candidates
       - Prefers merge over review when multiple candidates

    Use real venue names from Atlantic Canada: "Scotiabank Centre", "Scotiabank Centre Halifax", "Harbour Station", "Avenir Centre", "The Marquee Ballroom", "Casino New Brunswick".

    **GREEN phase: Implement `src/lib/scraper/venue-dedup.ts`**

    Exports:
    - `MERGE_NAME_RATIO = 0.15` (proportional edit distance threshold)
    - `MERGE_GEO_METERS = 100` (auto-merge geo threshold)
    - `REVIEW_GEO_METERS = 500` (log-for-review geo threshold)
    - `normalizeVenueName(name: string): string` — lowercase, trim, collapse whitespace, strip leading "The "
    - `venueNameRatio(a: string, b: string): number` — uses `distance()` from fastest-levenshtein on normalized names, returns `d / Math.max(na.length, nb.length)`
    - `DedupeDecision` type (union: merge | review | keep_separate)
    - `scoreVenueCandidate(incoming, candidate): DedupeDecision` — the two-signal gate per research spec
    - `findBestMatch(incoming, candidates[]): DedupeDecision` — iterates, returns best

    Key constraint per research: When incoming has `lat: null` or `lng: null`, the geo signal is unavailable. Even if name matches, route to review with reason `name_match_no_geo` — do NOT auto-merge without geo confirmation. This is the critical TM-specific behavior.

    Import `haversineDistance` from `@/lib/timelapse-utils` (do NOT rewrite). Import `distance` from `fastest-levenshtein`.

    **REFACTOR phase:** Clean up if needed, ensure all tests pass.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern venue-dedup</automated>
  </verify>
  <done>
    - venue-dedup.ts exports all scoring functions and types
    - venue-dedup.test.ts has 14+ test cases covering the full decision matrix
    - All tests pass
    - No database dependencies in the scoring module (pure functions only)
    - fastest-levenshtein installed in package.json
  </done>
</task>

</tasks>

<verification>
npm test -- --testPathPattern venue-dedup
</verification>

<success_criteria>
- `src/lib/scraper/venue-dedup.ts` exports venueNameRatio, scoreVenueCandidate, findBestMatch, normalizeVenueName, and threshold constants
- `src/lib/scraper/venue-dedup.test.ts` covers all decision matrix branches including the TM no-geo edge case
- All tests green
- No DB imports in venue-dedup.ts (pure scoring module)
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.5-phases/18-venue-deduplication/18-01-SUMMARY.md`
</output>
