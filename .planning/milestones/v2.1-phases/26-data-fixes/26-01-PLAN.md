---
phase: 26-data-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/venue-dedup-backfill.ts
  - src/lib/db/schema.ts
  - drizzle/0010_drop_phone_column.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0010_snapshot.json
  - src/lib/timelapse-utils.test.ts
  - src/lib/db/schema.test.ts
  - src/lib/filter-utils.test.ts
  - src/app/api/events/route.test.ts
  - src/lib/scraper/promote-source.test.ts
  - src/lib/scraper/orchestrator.test.ts
autonomous: true
requirements: [DATA-01, DATA-03]

must_haves:
  truths:
    - "venue-dedup-backfill.ts --execute delegates to performVenueMerge for all merge operations"
    - "phone column does not exist in venues or discovered_sources schema definitions"
    - "No application code or test file references a phone property on venues or discovered_sources"
  artifacts:
    - path: "scripts/venue-dedup-backfill.ts"
      provides: "Backfill script using performVenueMerge"
      contains: "performVenueMerge"
    - path: "src/lib/db/schema.ts"
      provides: "Schema without phone columns"
  key_links:
    - from: "scripts/venue-dedup-backfill.ts"
      to: "src/lib/db/merge-venue.ts"
      via: "import performVenueMerge"
      pattern: "import.*performVenueMerge.*merge-venue"
---

<objective>
Fix FK violation risk in venue-dedup-backfill.ts and remove dead phone column from schema.

Purpose: DATA-01 eliminates FK violations when running backfill in --execute mode by delegating to the battle-tested performVenueMerge function. DATA-03 removes the never-populated phone column from venues and discovered_sources tables.
Output: Safe backfill script, clean schema without phone column, migration SQL.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/venue-dedup-backfill.ts
@src/lib/db/merge-venue.ts
@src/lib/db/schema.ts

<interfaces>
<!-- performVenueMerge is the target function the backfill script must use -->

From src/lib/db/merge-venue.ts:
```typescript
export interface PerformVenueMergeOpts {
  canonicalId: number;
  duplicateId: number;
  candidateId: number;
  nameScore: number;
  distanceMeters: number | null;
  duplicateName: string;
  duplicateCity: string;
}

export async function performVenueMerge(
  opts: PerformVenueMergeOpts
): Promise<PerformVenueMergeResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor venue-dedup-backfill.ts to use performVenueMerge</name>
  <files>scripts/venue-dedup-backfill.ts</files>
  <action>
Replace the inline merge logic in the --execute branch (lines 191-242) with a call to performVenueMerge from src/lib/db/merge-venue.ts.

The current inline code manually reassigns events, reassigns scrape_sources, deletes the venue, and logs to venueMergeLog -- but it does NOT clean up event_sources FK references before deleting conflicting events, which causes FK violations.

performVenueMerge already handles this correctly (deletes event_sources rows before deleting conflicting events).

Key challenge: performVenueMerge requires a `candidateId` (from venue_merge_candidates table). The backfill script creates merge candidates in Step 6 (review logging) but auto-merges skip that. Solution: For each auto-merge, insert a venue_merge_candidates row with status='merged' BEFORE calling performVenueMerge, then pass that row's id as candidateId. Alternatively, since the backfill already logs to venueMergeLog separately, create a lightweight wrapper or modify the execute loop to:

1. Insert a venueMergeCandidates row for the pair (status: 'merged') to get a candidateId
2. Call performVenueMerge with the candidateId
3. Remove the now-redundant manual event reassignment, scrape_source reassignment, venue deletion, and merge log insertion code

Also remove unused imports that were only needed for the inline merge logic (events, scrape_sources if no longer directly used).

Keep the dry-run path, scoring logic, summary table, and review candidate logging unchanged.
  </action>
  <verify>
    <automated>npx tsx scripts/venue-dedup-backfill.ts 2>&1 | head -20</automated>
  </verify>
  <done>The --execute branch calls performVenueMerge for each merge candidate. No inline event/source reassignment or venue deletion code remains in the execute block. Dry-run mode still works.</done>
</task>

<task type="auto">
  <name>Task 2: Drop phone column from schema and all references</name>
  <files>src/lib/db/schema.ts, src/lib/timelapse-utils.test.ts, src/lib/db/schema.test.ts, src/lib/filter-utils.test.ts, src/app/api/events/route.test.ts, src/lib/scraper/promote-source.test.ts, src/lib/scraper/orchestrator.test.ts</files>
  <action>
1. In src/lib/db/schema.ts: Remove `phone: text('phone')` from the `venues` table definition (line 31) and `phone: text('phone')` from the `discovered_sources` table definition (line 164).

2. Generate a drizzle migration for the phone column removal. Run `npx drizzle-kit generate` which will create a new migration SQL file in drizzle/. The migration should contain two ALTER TABLE DROP COLUMN statements (venues.phone and discovered_sources.phone). If drizzle-kit does not auto-detect the change, manually create `drizzle/0010_drop_phone_column.sql` with:
   ```sql
   ALTER TABLE "venues" DROP COLUMN IF EXISTS "phone";
   ALTER TABLE "discovered_sources" DROP COLUMN IF EXISTS "phone";
   ```
   And update the journal and snapshot files accordingly.

3. Remove all `phone` references in test files:
   - src/lib/timelapse-utils.test.ts: Remove `phone: null` from venue fixture objects (lines ~59, ~333)
   - src/lib/db/schema.test.ts: Remove `'phone'` from column name arrays (lines ~16, ~117)
   - src/lib/filter-utils.test.ts: Remove `phone: null` from fixture (line ~51)
   - src/app/api/events/route.test.ts: Remove `phone: null` from fixture (line ~17)
   - src/lib/scraper/promote-source.test.ts: Remove `phone` from type definition (line ~42) and `phone: null` from fixture (line ~63)
   - src/lib/scraper/orchestrator.test.ts: Remove `phone: null` from fixture (line ~87)

4. Grep the entire src/ directory for any remaining `phone` references and remove them. Do NOT touch node_modules or unrelated files.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>phone column absent from schema.ts venues and discovered_sources definitions. No phone references in any src/ .ts file. TypeScript compiles cleanly. Migration file exists in drizzle/.</done>
</task>

</tasks>

<verification>
- `grep -r "phone" src/ --include="*.ts" | grep -v node_modules` returns no results
- `npx tsc --noEmit` passes with zero errors
- `npx tsx scripts/venue-dedup-backfill.ts` (dry-run) executes without errors
- `grep "performVenueMerge" scripts/venue-dedup-backfill.ts` returns a match
</verification>

<success_criteria>
1. venue-dedup-backfill.ts --execute mode delegates all merge operations to performVenueMerge (no inline FK-unsafe code)
2. phone column removed from both venues and discovered_sources in schema.ts
3. Migration SQL file exists for the phone column drop
4. Zero phone references remain in src/ TypeScript files
5. TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/26-data-fixes/26-01-SUMMARY.md`
</output>
