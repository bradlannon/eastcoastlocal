---
phase: 28-tests-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scraper/ticketmaster.test.ts
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "All 33 tests in ticketmaster.test.ts pass with zero failures"
    - "The .limit() mock chain resolves correctly for venueMergeCandidates existence check"
  artifacts:
    - path: "src/lib/scraper/ticketmaster.test.ts"
      provides: "Fixed mock chain for db.select().from().where().limit()"
  key_links:
    - from: "src/lib/scraper/ticketmaster.test.ts"
      to: "src/lib/scraper/ticketmaster.ts"
      via: "mock of db.select chain"
      pattern: "mockDb\\.select.*limit"
---

<objective>
Fix the 2 broken unit tests in ticketmaster.test.ts caused by an incomplete mock chain — `db.select().from().where()` is missing the `.limit()` method that production code calls at line 202 of ticketmaster.ts.

Purpose: Restore green test suite (TEST-01 tech debt item)
Output: All 33 ticketmaster tests passing
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/scraper/ticketmaster.test.ts
@src/lib/scraper/ticketmaster.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .limit() mock chain in ticketmaster.test.ts</name>
  <files>src/lib/scraper/ticketmaster.test.ts</files>
  <action>
The production code at ticketmaster.ts:187-202 calls `db.select({id: venueMergeCandidates.id}).from(venueMergeCandidates).where(...).limit(1)`. The test mocks `db.select` to return `{ from: () => ({ where: () => Promise.resolve([]) }) }` but `.where()` returns a bare Promise — no `.limit()` method.

Fix every location where `mockDb.select` is mocked (5 locations) so that `.where()` returns an object that is thenable AND has a `.limit()` method. The pattern:

```typescript
const mockWhereResult = Object.assign(Promise.resolve([]), {
  limit: jest.fn().mockResolvedValue([]),
});
// Then use mockWhereResult as the return from where()
```

Locations to fix (line numbers approximate):

1. **beforeEach** (~line 99): The default `mockDb.select` mock. Change `.where: jest.fn().mockResolvedValue([])` to return a thenable with `.limit()`.

2. **"runs fuzzy matching" test** (~line 446): Mocks city venues `[{ id: 55, name: 'Scotiabank Centre', ... }]`. The `.where()` for the city venues query returns those venues (no `.limit()` called). But the SECOND `db.select()` call in the same function invocation checks merge candidates and DOES call `.limit()`. Since the same mock handles both calls, make `.where()` return a thenable-with-limit for both. Use `mockReturnValueOnce` or make the limit always available. Simplest approach: always attach `.limit()` to the `.where()` return.

3. **"loads city venues using ilike" test** (~line 491): Uses a custom `whereCapture` implementation. The `.where()` mockImplementation must also return a thenable-with-limit. Change: `return Object.assign(Promise.resolve([]), { limit: jest.fn().mockResolvedValue([]) })`.

4. **"inserts review candidate" test** (~line 518): Same as #2 — city venues returned, then merge candidates `.limit()` called. Fix identically.

5. **"creates new venue without fuzzy log" test** (~line 545): Empty city venues, no merge path reached, but add `.limit()` for consistency.

IMPORTANT: For tests that trigger the review path (tests at ~446 and ~518), the `.limit()` mock must resolve to `[]` (no existing merge candidate found) so the code proceeds to insert the merge candidate row. This matches the test expectations.

Do NOT change any test assertions or production code. Only fix the mock chain shape.
  </action>
  <verify>
    <automated>npx jest ticketmaster 2>&1 | tail -10</automated>
  </verify>
  <done>All 33 tests pass (Tests: 33 passed, 33 total). Zero failures. Exit code 0.</done>
</task>

</tasks>

<verification>
Run `npx jest ticketmaster` — expect "Tests: 33 passed, 33 total" with exit code 0.
</verification>

<success_criteria>
- ticketmaster.test.ts: 33 passed, 0 failed
- No skipped tests
- Exit code 0
</success_criteria>

<output>
After completion, create `.planning/phases/28-tests-and-validation/28-01-SUMMARY.md`
</output>
