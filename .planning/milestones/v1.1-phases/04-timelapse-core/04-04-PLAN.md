---
phase: 04-timelapse-core
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - TIME-01
  - TIME-03
  - TIME-04
  - MODE-01
  - MODE-02
  - MODE-03
  - HEAT-01
  - HEAT-04

must_haves:
  truths:
    - "User can click the mode toggle and see heatmap replace pin clusters"
    - "User can drag the scrubber and see heatmap intensity change without flicker"
    - "User can play and watch the scrubber auto-advance with heatmap animating"
    - "Date/time label shows correct day and block name as scrubber moves"
    - "Sidebar event list updates to match the current 24-hour time window"
    - "Map zoom/pan is preserved when toggling between modes"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete timelapse feature works end-to-end through manual visual testing.

Purpose: HEAT-04 (smooth updates), MODE-01 (layer switching), MODE-03 (viewport preservation), and TIME-01/TIME-04 (scrubber interaction) cannot be verified through unit tests -- they require a running browser with a Leaflet map instance. This checkpoint catches visual regressions, interaction bugs, and integration issues before the phase is marked complete.

Output: Confirmed working timelapse mode or specific issues to address.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-timelapse-core/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for manual verification</name>
  <files></files>
  <action>
    Run `npm run dev` to start the Next.js development server. Confirm it starts without errors and outputs the local URL (typically http://localhost:3000).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000</automated>
  </verify>
  <done>Dev server running and responding with 200</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete timelapse feature</name>
  <action>
    Present the following verification checklist to the user. The dev server is running at http://localhost:3000.
  </action>
  <verify>
    <automated>MISSING -- checkpoint:human-verify requires manual browser testing</automated>
  </verify>
  <done>User confirms all 8 verification checks pass or reports specific issues</done>
  <what-built>Complete heatmap timelapse mode with mode toggle, scrubber bar, play/pause animation, and sidebar sync</what-built>
  <how-to-verify>
    Open http://localhost:3000 in your browser and test:

    1. **Mode Toggle (MODE-01):** Click the heatmap toggle button (top-right of map). Verify:
       - Pin clusters disappear
       - Blue-to-red heatmap gradient appears showing event density
       - TimelineBar scrubber appears at bottom of map with frosted glass overlay

    2. **Scrubber Drag (TIME-01, HEAT-04):** Drag the range input scrubber across its full range. Verify:
       - Heatmap intensity changes smoothly (no flicker or canvas teardown)
       - No visual glitches or blank frames during drag

    3. **Date Label (TIME-03):** While dragging, verify the date/block label updates:
       - Shows format like "Sat Mar 14 - Evening"
       - Block cycles through Morning/Afternoon/Evening/Night

    4. **Sidebar Sync (MODE-02):** While dragging, check the sidebar event list:
       - Event list updates to show only events in the current 24-hour window
       - Event count badge on TimelineBar matches sidebar count

    5. **Play/Pause (TIME-04):** Click play button:
       - Scrubber auto-advances at ~1 second per step
       - Heatmap animates with each step
       - Click pause -- scrubber stops
       - Drag scrubber during playback -- playback auto-pauses

    6. **Viewport Preservation (MODE-03):** Zoom into a specific city, note the zoom level:
       - Toggle to cluster mode -- verify zoom and pan unchanged
       - Toggle back to timelapse -- verify zoom and pan still unchanged

    7. **Province Filter:** Select a province filter (e.g., NB):
       - Verify heatmap only shows events in that province
       - Toggle back to cluster mode -- verify province filter still active

    8. **Cleanup Check:** Toggle between modes 5-10 times rapidly:
       - No ghost heatmap layers visible in cluster mode
       - No performance degradation
       - Open DevTools Console -- no errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
All 8 manual verification checks pass without issues.
</verification>

<success_criteria>
- Human confirms all visual/interaction behaviors work correctly
- No console errors during mode toggling and scrubbing
- No ghost layers or memory leak symptoms
- Sidebar stays in sync with heatmap time window
</success_criteria>

<output>
After completion, create `.planning/phases/04-timelapse-core/04-04-SUMMARY.md`
</output>
